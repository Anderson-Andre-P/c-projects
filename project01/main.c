#include <stdio.h>
#include <stdlib.h>

int main()
{
    int A; // int == integer
    printf("Write an value: ");
    scanf("%d", &A); // %d is is used with int values
    printf("Reported value: %d", A);

    return 0;
}
